export type ThreadItem = {
    id: string;
    title: string;
    source: string;
};

export type StackItem = {
    id: string;
    title: string;
    source: string;
};

export type SnippetItem = {
    id: string;
    title: string;
    source: string;
};

export type VideoItem = {
    id: string;
    title: string;
    source: string;
    url: string;
};