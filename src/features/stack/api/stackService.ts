import type { StackItem } from "@/app/fakeTypes";

export async function searchStacks(query: string): Promise<StackItem[]> {
    // TODO: implement real API call
    console.log("searchStacks", query);
    return [];
}
